@using Microsoft.AspNetCore.Http.Extensions
@model OwnStream.Database.Models.DatabaseWebhook

@{
	ViewBag.Title = Model.Name;
	Layout = "_Layout";
}

<h2>@Model.Name</h2>
<p>
	<b>URL: <code>@(Context.Request.GetDisplayUrl().Split("/Webhook/Detail")[0])/api/webhook/@Model.Id</code></b>
</p>
